//~ run = "${PION} elab ${FILE}"

let FstType = fun ((A, B) : (Type, Type)) ((a, b) : (A, B)) -> A;
let SndType = fun ((A, B) : (Type, Type)) ((a, b) : (A, B)) -> B;

let fst : FstType = fun (A, B) (x, y) => x;
let snd : SndType = fun (A, B) (x, y) => y;

{}
